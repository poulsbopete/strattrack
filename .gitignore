# Generated for npm pack/publish (source of truth remains docker/)
mcp/strattrack-drawers-index.json

# Dependencies
node_modules/
.venv-migrate/

# Environment and secrets
.env
.env.*
!.env.example

# Salesforce → Elasticsearch poll worker (local high-water marks)
.strattrack-sfdc-watermark.json

# Thursday 1-2-3 generator output (see docs/THURSDAY_123_CRON.md)
thursday-123-output/

# Logs and OS
*.log
npm-debug.log*
.DS_Store

# IDE
.idea/
.vscode/
# Cursor MCP — copy .cursor/mcp.json.example to .cursor/mcp.json locally
.cursor/mcp.json
*.swp
*.swo

# Build / coverage (includes built .mcpb from scripts/build-strattrack-mcpb.sh)
dist/
build/
coverage/
.nyc_output/

# Docker / local data (adjust if you version docker volumes)
*.local.sql
data/
