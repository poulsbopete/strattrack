# StratTrack local Elasticsearch — extend the official image for reproducible builds.
# Add plugins or ship config here when needed (e.g. analysis-icu, synonym files).

FROM docker.elastic.co/elasticsearch/elasticsearch:8.17.2

# Run as the default elasticsearch user from the base image.
